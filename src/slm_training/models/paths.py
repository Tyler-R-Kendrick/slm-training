"""Canonical checkpoint paths (fixtures are committed; outputs/ is local-only)."""

from __future__ import annotations

from pathlib import Path

# Demo checkpoint for the web playground — committed under fixtures/checkpoints/.
PLAYGROUND_DEMO_CHECKPOINT = Path("fixtures/checkpoints/playground_demo/last.pt")
