name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  python:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: |
            package-lock.json
            tools/openui_bridge/package-lock.json
            tools/design_md_bridge/package-lock.json
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev,mcp,web]"
      - run: npm ci
      - run: npm ci
        working-directory: tools/openui_bridge
      - run: npm ci
        working-directory: tools/design_md_bridge
      - run: ruff check .
      - run: python -m compileall -q src scripts tests
      - run: pytest -q
      - name: Verify documented disjoint data build
        run: |
          python -m scripts.build_train_data --source all --version ci --synthesizer none
          python -m scripts.build_test_data --source both --version ci --train-manifest outputs/train_data/ci/manifest.json
