interface:
  display_name: "Autotrain OpenUI SLMs"
  short_description: "Continuously improve OpenUI models and harnesses"
  default_prompt: "Use $autotrain in its continuous local-only mode: keep a persistent goal, fetch latest between bounded campaigns, improve the model or evidence-implicated canonical harness, and print the verified result matrix between runs. Use one finite pass only when I name a phase or say --once."
