# E112 E91-recipe reproduction (2026-07-15)

E112 attempted to reproduce E91's recorded recipe: 192-dimensional model,
6 heads, 3 context layers, 6 denoiser layers, frozen context, structural bias
2.5, mixed masking, schema context, LTR weight 2.0, fidelity weight 1.5, and
16 generation steps.

The bounded training process terminated after reaching step 359 without
writing `train_summary.json` or a checkpoint. The durable `metrics.jsonl`
contains training telemetry through that step (about 303,822 prompt tokens and
120,107 target tokens). Because the run has no complete checkpoint or final
loss summary, no evaluation was run and no quality claim is made.

Decision: incomplete/resource-terminated diagnostic. Preserve the telemetry;
future reproduction should use a smaller batch, explicit resource monitoring,
or resume from a durable full-state checkpoint.
