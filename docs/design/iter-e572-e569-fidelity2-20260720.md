# E572 — E569 fidelity-loss weight 2

E572 warm-starts E569 for 48 matched CPU steps while changing only placeholder
fidelity loss from 0.5 to 2.0. Training completes in 84.26 seconds with 2,561
target tokens and writes a local scratch checkpoint
(`bb6a58ff…cc29efa2`).

On matched OOD `n=4`, fidelity improves 0.2583→0.6500, validity
0.5550→0.7900, and reward 0.6920→0.8170. This is not a semantic improvement:
structure falls 0.2031→0.1438, component recall 0.3333→0.1458, AST-node F1
0.3389→0.1833, and meaningful-v1 0.25→0. Binding-aware meaningful-v2 remains
0 and AgentV remains 0/1. Required-placeholder misses fall from four records
to three, but semantic-role mismatches and placeholder spam appear.

**Verdict:** reject as a semantic candidate and keep local only. Fidelity
weight 2 is a non-semantic placeholder/reward Pareto, not a strict-meaning
fix. No sync or promotion. Evidence:
[JSON](iter-e572-e569-fidelity2-20260720.json).
