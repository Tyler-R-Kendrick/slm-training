# Autonomous measurement integration — 2026-09-07

## September 8: native supervised integration corrections

The new `measurement_supervised` owner locks a controlled public inference-only
fixture and invokes the real supervisor operation, driver cycle context and
`autoresearch.cmd_run` command cursor. It does not replace a driver, child,
AgentV result or metric with a mock. Original dirty training provenance remains
dirty; ordinary clean-source training replay remains unchanged. Preparation now
resolves actual `origin/main` rather than incorrectly recording HEAD as upstream.
Only the validated diagnostic contract selects content-bound authoring source;
normal continuous launches retain their clean tracked-worktree requirement.

Actual attempt `autonomy-mea-supervised-v169-r7-20260908-c0001` reached loss and
two decode invocations. Both invocations decoded the first two rows because the
engine's decreasing invocation allowance changed `evaluation_policy_sha256`.
The original partial is retained under `rejected_partials`. The fix separates
invocation scheduling wall from partial-row semantic identity, while keeping
per-record timeout, seed, scorer, selection and bundle binding. Legacy partial
policy digests cannot silently acquire the new compatibility semantics.
Required version composition: `harness.model_build.eval` v110; no threshold,
loss-estimator formula, frozen analysis plan or ship gate changes.

Actual successor `autonomy-mea-supervised-invocationfix-20260908-c0001` advanced
the control through all six rows across three real decode chunks. Its supervisor
process consumed 64.460733674 seconds, then finalization rejected concurrent
**source** drift (the environment matched). No terminal success, paired result,
promotion or shipment is claimed for this interrupted source identity. Earlier
controller process receipts are also retained: first attempt 41.069164275s,
v169-r3 34.493100432s, v169-r4 15.176732765s, v169-r7 31.747950409s.
These enclosing process costs overlap their nested evaluation costs and must
not be added to them as independent resource charges. Other prepared namespaces
refused before launching any supervisor operation. No files were deleted.

Latest focused verification: **37 passed in 7.98 seconds**, exit 0, with
`-o addopts= -m '' -p no:cacheprovider`, covering measurement bundle/fixture,
supervised collection contracts and the actual evaluator's invocation-budget
regression (existing mocked-clock decoder; not a neural capability claim).
JUnit: `outputs/runs/autonomy-validation/measurement-supervised-final-20260908.xml`,
SHA256 `fbdf4ef43fc18a67c2643dcafc06195d4d379a466f251581256fb10aae6f70f3`.
The original nine resume tests plus that new regression separately passed
(10 tests, 14.14s). The new test lives in a disjoint module; the original resume
module is 504 lines, `measurement_supervised.py` is 400, and `autoresearch.py`
is 2111. No quality baseline was raised. Test-case resources were generated by
`scripts.extract_test_cases --write`, and the shared fixture is loaded through
`pytest_plugins` in either collection order.

The direct report's documentation serializer now emits project-relative artifact
references. Its controller evidence is unchanged; documentation JSON is a
portable rendering, not byte-identical to the original run JSON. Documentation
SHA256 is `f331cb21244ecab02e2b2dee6777e08d41087587724eda82ee65eeae9438d226`.
The original controller result digest below still identifies the untouched file.

Finite supervised invocation (same environment/cap as below; no service):

```text
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_supervised prepare --run-id <fresh-id>-c0001 --loop-id <fresh-id> --enable-fixture-experiment
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_supervised run --run-id <fresh-id>-c0001 --loop-id <fresh-id> --enable-fixture-experiment
```

The run command validates current source and environment before each bounded
operation. A completed native handoff triggers independent artifact collection
and a portable JSON/Markdown report. A typed yield remains pending under the
same campaign; a changed source requires a successor identity, not a rewritten
lock. As of this entry the native two-arm completion obligation remains open.

## September 8: reusable finite current-endpoint acceptance

Current direct execution completed in
[`autonomy-measurement-autonomy-mea-resolved-endpoint-20260908-r2.json`](autonomy-measurement-autonomy-mea-resolved-endpoint-20260908-r2.json)
and its matching markdown. Both arms have **six decoded cases and six paired
loss observations**. Eight actual bounded child invocations consumed
**92.731496359 seconds**. Loss exits were 0; decode exits were 10, 10, 8 per
arm. The final exit 8 is completed ship-gate rejection, not missing evidence.
Four actual AgentV SDK runs each reported zero execution errors; each SDK
envelope contains **one aggregate benchmark**, not six SDK documents. Six-case
counts come from the domain's validated selection and per-record evidence.

The unchanged driver consumer returned `supported_diagnostic_benefit`:
six non-tied root-family pairs, mean paired reduction 0.7785168020,
median 0.7739877145, paired SD 0.0496898233, two-sided sign p=0.03125.
The token-weighted means were 25.5114916348 (control) and 24.7525203287
(candidate); their difference is not the equally weighted mean case delta.
These are conditional masked-token cross-entropy observations, not exact
generative likelihood. Parse rate was 1.0 for each arm; meaningful-program
rate was 0 versus 1/6, binder-reference F1 tied at 0.9444444444. No independent
confirmation, adequate-power claim, champion promotion, shipment, or fresh
training occurred. The raw diagnostic decision is retained, including its
`stack_layer=true` field; the fixture invokes no delivery/promotion API and
its envelope explicitly denies that authority. Fixture-local observed-SD
adaptation was skipped because that optional expectations file was absent;
no global calibration file was changed.

This proves **direct CLI → loss/decode → canonical pairing/disposition**.
It does **not** prove the actual supervisor/`cmd_run` cursor path. That distinct
MEA-PIPELINE obligation remains for shared-file integration and execution.

Final focused verification after execution: **98 passed in 3.91 seconds**,
zero skipped/deselected, exit 0, selecting the four focused files listed below
plus the unchanged
`tests/test_autoresearch/test_autonomy_evidence_boundary_adversary.py`.
JUnit `outputs/runs/autonomy-validation/measurement-current-pipeline-final-20260908.xml`
has SHA256 `4a2927c86eba9e07e8e5b6f117f1cd20e6c1ae6557b25bb78f3341a172fa30ce`.
The independent adversary file still hashes to
`2bc73c52897dd608bb8bdf9e21ddf33349e424b0ff1fae1c5d024d0999e18c25`.
A separate read-only verification passed the 11-event chain, all eight
event-bound child receipts, both six-row arms, current evaluator identity,
AgentV artifacts and identical run/docs JSON. Result JSON SHA256 is
`5596fef46ef1aed708b2e4af775c1dd3dcb49b55a40acae28a8313846860d356`;
the locked plan file SHA256 is
`3343a89ea13965e495241e6fc33d4dae5245662dfb89469a4688b085b2cf47b1`.
File-byte hashes differ from canonical event payload hashes by serialization;
each was verified against its respective owner, not interchanged.

Preparation and verification evidence:
[`autonomy-measurement-finite-preparation-20260908.json`](autonomy-measurement-finite-preparation-20260908.json).
The historical six-case endpoint mismatch remains rejected. This addition never
edits its locked endpoint or accepted/rejected events.

The executable owner is
`slm_training.harnesses.experiments.autonomous_learning.measurement_fixture`.
It locks a **new** `ExperimentCampaignV1` using the policy-resolved screening
primary (`smoke.eval_nll`, decrease, minimum effect 0.02, diagnostic). Its
three files separate finite execution, typed CLI compilation and artifact
verification; no second evaluator, promotion engine or campaign store exists.
`compile_commands` supplies the actual decode command, including full selected
six, seed 7301, honest constrained tree decode and two-record chunk limits.
Only its evaluation command executes: the compiled train command is explicitly
recorded as not executed because compatible retained checkpoints are reused.
Loss is the real `scripts.evaluate_loss_suites` CLI, six smoke cases, mask seed
7301, raw conditional masked-token cross entropy, not exact generative NLL.

Both retained immutable bundles and dataset manifests validate. Both checkpoints
load in the current TwoTower implementation and each has **65,826 trainable
parameters**. No new training, checkpoint, neural-quality measurement, service,
remote write or capability claim occurred in these preparation probes. The six
cases retain their exact case/input/root ordering; they are public regression
cases, not independently access-controlled confirmation data.

The finite execution budget is eight child invocations: one loss and three
two-record decode invocations per arm. Every child uses the canonical bounded
process owner with 170-second interrupt and 10-second grace. Reservations are
durable before launch; rerunning the same activity refuses to mint another
attempt budget. A lost terminal receipt remains an unresolved reserved attempt,
not permission for blind rerun. This fixture is not a multi-host lease or a
general crash-reconciliation demonstration. Exits 10 are pending; final exit 8
can be a completed ship-gate rejection, but only valid complete artifacts permit
collection. A current-source/campaign mismatch refuses reuse.

Collection validates exact complete case coverage, checkpoint identity, current
evaluator stamps/digests, loss rows and actual AgentV manifests/results. It then
calls `run_arm_eval_nll`, `read_paired_nll` and the driver's `_classify_positive`
against the new lock. Calibration writes stay campaign-local. The returned
diagnostic disposition is retained unchanged; no confirmation, promotion or
shipment API is invoked. JSON and matching markdown closeout are emitted under
`docs/design/autonomy-measurement-<campaign-id>.*` when collection succeeds.

Executed checks (all bounded, exit 0): **86 tests passed**, no skips/deselections,
with `pytest -q --tb=short -o addopts= -m '' -p no:cacheprovider` selecting
`tests/test_harnesses/experiments/test_measurement_fixture.py`,
`tests/test_scripts/test_locked_measurement_endpoint.py`,
`tests/test_scripts/test_autotrain_nll_integration.py`, and
`tests/test_harnesses/model_build/test_measurement_contract.py`.
JUnit: `outputs/runs/autonomy-validation/measurement-fixture-integrated-20260908.xml`;
pytest console elapsed 8.06 seconds. Ruff and targeted complexity checks passed.
These tests include simulated child results and synthetic scoreboard contracts;
they are not substituted for actual two-arm evaluation.

The initial `prepare` CLI probe exited **1 before locking/launching workloads**:
`release version pending: evals.scoring needs v28, found v27`.
INTEGRATOR owns the required registry composition: evaluator v109 (now present),
scoring v28, and experiments v169 or later. The new modules are watched by the
existing `harness.experiments` directory ownership. No frozen loss-suite, scorer
formula or scientific threshold change is needed. Following parent direction,
the fixture now requires evaluator v109 but does not wait for unrelated final
registry composition. The actual completed run records scoring v27,
experiments v168, loss-suite v2 and model v331 plus exact relevant source hashes;
later version composition must not rewrite these historical identities.

The first new campaign (`autonomy-mea-resolved-endpoint-20260908`) encountered
real source drift: driver SHA256 changed from
`47eb117b34be6b4b935b52454be55387d98359039706513393e0d9fe4122e3a6`
to `f591118fb60ec82d1082b5b6a1b46a33885a4846bf4f86a5a640c1941f596116`.
The candidate-decode1 invocation refused before reserving/launching a child.
Its five completed child receipts (57.032683384 seconds) and control's complete
scoreboard remain preserved. Nothing was imported into the successor result.
The successful `-r2` campaign freshly executed all eight workloads. Across both
namespaces, **13 actual child attempts consumed 149.764179743 seconds**;
parent/import/validation work is additional wall time, not hidden as child cost.

### Supervisor integration handoff (not yet executed)

Use the immutable plan at
`outputs/autoresearch/autonomy-mea-resolved-endpoint-20260908-r2/measurement_fixture.json`.
Its canonical manifest digest is
`7bc61bcbd50354ee844b115291b2ae73fc8ed151f34e44ae5b8720e44af5ad57`;
selection digest is
`11ccffe86a6cf32ed1788721a39e6f1818fb91a0b9cedd0560a8ee4c3f29c3ff`.
`inputs` contains exact bundle/checkpoint/data hashes and ordered case/root/input
IDs; `arms` contains compiled commands, explicit checkpoint paths and seed 7301.
`primary` records the actual policy-resolved diagnostic endpoint and direction.
Use these as inputs for a **new pre-execution supervisor lock**, not by editing
the completed direct campaign or copying its scored rows into fresh receipts.

The current integration seam is `autotrain_cycle_prepare._arm` →
`autoresearch.cmd_run` → its persisted command cursor, entered through
`autotrain_supervisor_operations.run_operation(operation="driver")` and its
real worker. `_arm` currently routes checkpoint reuse through
`autoresearch._prepare_reused_training`. That helper is deliberately restricted
to clean-source frozen replay lineage: it rejects dirty source manifests and
requires `train_summary.version_stamp.code_dirty is False`. Both retained
scratch summaries truthfully have `code_dirty=true`, and the original retained
campaign has no replay manifests. Do not invent clean manifests, relabel those
summaries, bypass the guard, or silently retrain. A parent-owned explicit
diagnostic immutable-bundle remeasurement contract is needed at this seam;
it must retain original source/lineage exposure and prohibit promotion. The
existing clean replay policy stays unchanged. All final child commands, cursor
events, supervisor leases and paired disposition must be real, separately
budgeted, and bound to the new lock; controlled fixture preparation is labelled
as such, never claimed as ordinary autonomous proposal selection.

### Exact finite invocation

From `outputs/workspaces/autonomy-candidate`, retain the authorized local
dependency setup (no install/copy needed):

```bash
export PYTHONPATH=src:outputs/runs/autonomy-validation/dependencies
export OMP_NUM_THREADS=1 MKL_NUM_THREADS=1
export OPENUI_BRIDGE_CLI=/home/codex/repos/slm-training/src/apps/openui_bridge/cli.mjs
export AGENTV_RUNNER=/home/codex/repos/slm-training/scripts/run_agentv_eval.mjs
export PATH=/home/codex/.nvm/versions/node/v22.23.1/bin:$PATH
```

Each line below is a separate bounded
invocation. Use `/home/codex/repos/slm-training/.venv/bin/python` for `python`,
prefixing each with `rtk proxy timeout -s INT -k 10 170`.
The completed successor used `--run-id autonomy-mea-resolved-endpoint-20260908-r2`
in every line. Reproduction requires a fresh run ID: existing activities cannot
be reset into a new budget.

```text
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture prepare --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity control-loss
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity control-decode1
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity control-decode2
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity control-decode3
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity candidate-loss
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity candidate-decode1
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity candidate-decode2
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture run --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment --activity candidate-decode3
python -m slm_training.harnesses.experiments.autonomous_learning.measurement_fixture collect --run-id autonomy-mea-resolved-endpoint-20260908 --enable-fixture-experiment
```

The defaults resolve the existing validated
`outputs/runs/autonomy-mea-data/final-evidence.json`,
`autonomy-mea-train-admitted-v1` and `autonomy-mea-smoke-successor-v1` snapshots.
For another checkout, supply those retained local artifacts explicitly via the
tested `--retained-evidence`, `--train-version` and `--eval-version` arguments;
their absence is not silently repaired with different cases or checkpoints.
The parser's actual `--help` was executed successfully. No `/tmp` script is
needed. Explicit source drift after preparation requires a fresh campaign lock,
not modification of a frozen plan.

Base: `e0eca9f9910244ecc20eb480d363f1852417b599`; audit baseline:
`2c4e2df3f9b9bfb6efe13e88108a57ea2ac9f64c`. Measurement-owned source is
unchanged between these revisions. No service is activated by this work.

## Retained v108 completion — independently inspected September 7 local

This supersedes the earlier pending-v108 statements below, not their historical
failure evidence. The retained `autonomy-mea-data-final-20260907` comparison
completed both direct and interrupted/chunked paths. The coordinator reran the
actual cache-completeness and bundle validators and paired-statistics owner in a
read-only check. AgentV's declared directory/files exist and its recorded SDK
execution-error count is zero. No model was retrained during this inspection.

| Arm | Selected / decoded | Parse | Meaningful program | Binder F1 |
| --- | --- | --- | --- | --- |
| control | 6 / 6 | 1.0 | 0.0 | 0.9444444444444445 |
| candidate | 6 / 6 | 1.0 | 0.16666666666666666 | 0.9444444444444445 |

The exact ordered case/input/root/seed and prediction identities match the
direct versus chunked records for each arm. Latencies are separate measurements,
not claimed equal. The interrupted control retained two decoded rows; uncommitted
metrics were recomputed, with no retraining. Every arm has a validated immutable
checkpoint bundle and exact continuation state. The fixture trained three CPU
updates per arm, batch two, seed 7301, width 32, one context/denoiser layer,
65,826 trainable parameters, eight admitted train-only records, six consumed
examples and 255 target tokens per arm. Checkpoint digests and all command
receipts are in `outputs/runs/autonomy-mea-data/final-evidence.json` (SHA256
`09fa9c04c0c7564803e285c637eb39b1ce87b63cf71a4f8bb1708d4ecd52ee38`).

Binder pairs are all tied: six declared public root families, zero non-tied
pairs, p=1, **inconclusive**. The historical masked-token cross-entropy diagnostic
reported a favorable mean paired reduction of 0.7785168020 on six broad cases
(two-sided sign p=0.03125); that is not exact generative NLL, a power analysis,
independent full-training replication, or confirmation of semantic improvement.
The two arms share the same initialization seed. Both failed ship gates.
No confirmation, promotion, shipment, or production-capability claim is made.

These observations retain model v330 / evaluator v108 / loss-suite v2 identities.
Current ONNX v331 fixes do not relabel the retained results or establish ONNX
quality. The current read-only command and its result are under
`coordinator_update.measurement_inspection` in
`autonomy-validation-restored-20260907.json`. Its first check incorrectly treated
the AgentV `runDir` as a file; the corrected check requires that entry to be a
directory and all other declared artifacts to be files. Nothing was fabricated
or republished to make that check pass.

## Integration interface (work in progress)

Historical sections below describe the pre-recovery workspace and receipts from
surviving session logs, not current-checkout verification. Their checkpoint and
evaluation payloads were lost. Current replay verification is recorded separately
at the end; no old checkpoint or old partial row is reused.

The existing `evaluate_suites` / `evaluate` owners remain authoritative.
They accept `partial_scoreboard`, `resume_from`, `max_records_this_run`;
the CLI accepts `--eval-limit`, `--partial-scoreboard`, `--resume-run`, and
`--max-records-this-run`. `compile_commands` already forwards subset and
chunk knobs. INTEGRATOR should consume exit 10 as pending measurement and
check explicit completeness before confirmation. No shared schema edit is
requested yet.

MEA-PATH priority: actual CPU TwoTower direct and compiled six-record
evaluation on the certified corpus. ARTIFACTS owns training state; please
provide a fresh current-contract checkpoint and tokenizer sidecars for this
test (train-only `openui_verified_train_v2` preferred). No trainer edits here.

MEA-ROWS/RESUME: verify exact selected IDs, reject empty/duplicate selection,
bind resume to model bundle and scorer/selection identities, retain original
partial evidence on incompatibility. MEA-DECISION/PUBLISH: distinguish pending
rows from decoded completeness, reject sentinel cache hits, and defer AgentV
until the applicable measurement finishes. Numeric ingestion will reject
nonfinite metrics and invalid rates.

Initial capability observation: this authoring snapshot has no root
`node_modules`. AgentV publication must be probed using available dependency
resolution before interpreting any real CLI run as complete.

## First executed counterexample and repair verification

The original six-case direct command reached `_score_one` then raised
`RuntimeError: Install bridge deps` from `data.verify.stack._canonical` via
`dsl.lang_core`. Exit 1, no scoreboard. Retained log:
`outputs/runs/autonomy-mea-six-base/direct-eval.log`. This reproduces a concrete
missing-scoreboard producer failure; it does not establish that every historic
missing scoreboard had the same cause. The owner is bridge readiness, not
`eval_limit` propagation. OPERATIONS should probe the actual canonical bridge
before starting expensive work.

The existing `OPENUI_BRIDGE_CLI` and `AGENTV_RUNNER` overrides reuse the installed
host dependencies without copying caches or changing the human checkout. The
real bridge probe validates `root = TextContent(":slot_0")`; the real SDK imports.
The first all-at-once replay exceeded the hard cap (exit 137) and is not quality
evidence. Subsequent invocations lock the same first six of 96 records and use
the existing two-record chunk interface.

Fresh checkpoint: `outputs/runs/autonomy-mea-six-base/checkpoints/last.pt`, CPU
scratch TwoTower, seed 7301, 3 updates, batch 2, width 32, one context/denoiser
layer, lexer output, 88,674 trainable parameters; 893 certified train records;
6 consumed examples and 422 target tokens. Training exit 0. Explicit local
`--no-sync-checkpoints`; no promotion or ship claim. INTEGRATOR owns the card
and README entry; ARTIFACTS may reuse this checkpoint without retraining.

The initial actual-owner regression run with the working bridge produced
10 failures / 6 passes (numeric fixes had already been loaded by this second
probe); the initial no-bridge run additionally reproduced NaN/infinity acceptance.
After the measurement guards, the same 16 tests pass in 2.79 seconds.
These are stub-decoder contract tests, not real trained-model quality evidence.

Integration/version request: bump `harness.model_build.eval` for
`eval_runner.py` and its new responsibility extraction `eval_measurement.py`;
bump the owning autoresearch component for `autotrain_metrics.py`. Exit 10
now identifies every resumable incomplete CLI invocation, even without
threshold flags. No scientific metric formula, gate, frozen data, or model
parameter budget is changed. The exact immutable partial schema name remains
`EvalPartialScoreboardV1`; strengthened identity fields are additive and old
partials lacking them are archived/refused for reuse, never relabelled current.

## MEASUREMENT handoff (17:18 UTC)

All five leaves have integrated owner-local changes and real-owner regressions.
MEA-PATH remains acceptance-incomplete: actual direct and compiled CLI workloads
both stall scoring record five; no completed six-case decoded scoreboard is claimed.
Faulthandler localizes this to `data.quality._prompt_component_requirements`'s
eager tuple expansion. The exact frozen case is
`program_748f040f0cc63303__e54b667a`: numeric identifier
`generated_945137945819` is interpreted as a component count. DATA owns that fix;
it changes evaluator semantics and requires a successor measurement identity.
The source digest now includes `data/quality.py` so pre-fix partial rows cannot
silently authorize post-fix comparisons. Original partials are archived on refusal.

Parent wiring: `compile_commands` must propagate the locked `seed` to
`evaluate_model` (currently evaluation defaults to zero despite training seed
7301). Existing limit/partial/max-records flags already propagate. No engine,
supervisor, shared-schema, registry, or promotion file was edited by this lead.

Suite metrics expose `selected_record_ids`, `selected_root_ids`, ordered
`input_sha256s`, `selection_sha256`, and `evaluator_sha256`. Details/partial rows:

```json
{"id":"case", "case_id":"case", "root_id":null,
 "input_sha256":"sha256", "seed":7301,
 "estimator_id":"constrained_decode/<evaluator digest>",
 "evaluator_sha256":"sha256", "selection_sha256":"sha256"}
```

Only explicit `meta.root_family_id`/`meta.root_id` supplies a root; null means
unverified independence. Diagnostic loss reports expose the same selection in
`selection`, the estimator ID at top level, and `per_record` rows also containing
`nll`, `nll_sum`, `masked_tokens`, and `units=nats_per_masked_token`. This is
conditional masked-token cross entropy, not exact generative sequence NLL.
STAT's attachment should preserve this selection/row evidence, not infer pairs
from sorted numeric maps. `read_eval_nll_records` now rejects the entire payload
when any value is invalid; invalid pairs cannot vanish from both arms.

Executed stable-source shard (exit 0):

```text
timeout -s INT -k 10s 170s env PYTHONPATH=/tmp/slm-autonomy-20260907/src
 PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1
 LANGSMITH_TRACING=false
 AGENTV_RUNNER=/home/codex/repos/slm-training/scripts/run_agentv_eval.mjs
 OPENUI_BRIDGE_CLI=/home/codex/repos/slm-training/src/apps/openui_bridge/cli.mjs
 /home/codex/repos/slm-training/.venv/bin/python -m pytest -o addopts= -q
 tests/test_harnesses/model_build/test_measurement_contract.py
 tests/test_harnesses/model_build/test_eval_runner_resume.py
 tests/test_evals/test_denoising_nll.py
 tests/test_harnesses/model_build/test_eval_gates.py
```

Result: 110 passed in 21.09 seconds. Includes actual tiny-model denoising-loss
tests, mocked-clock/stub-decoder interruption tests, real SDK publication paths,
same-source chunk replay, writer exclusion, stale selection/seed/tokenizer refusal,
raw-decode persistence before scoring failure, numeric and sentinel-cache guards.
An earlier overlapping source-edit run produced 29 passes/one identity-refusal
failure; the stable rerun above passes. This is not universal chaos verification.

Actual new loss CLI run (same environment, exit 0): `python -m
scripts.evaluate_loss_suites --checkpoint outputs/runs/autonomy-mea-six-base/checkpoints/last.pt
--test-dir src/slm_training/resources/data/eval/e938_role_safe_all_targets_smoke96_v2
--base-suite smoke --limit 6 --mask-seed 7301 --no-legal-support
--out outputs/runs/autonomy-mea-six-base/loss_suites_identity.json`.
Weighted diagnostic loss 24.545590017978768; all configured categories present;
actual AgentV publication. No paired treatment benefit or champion promotion.

Ruff check of measurement owners/tests: exit 0. Required component update requests:
`harness.model_build.eval`, `evals.loss_suite`, owning autoresearch scripts.
The loss definition gains locked selection content/ordering (successor digest);
historical definitions and raw NLL formulas remain unchanged. Per-chunk randomness
is now recorded/stable for identical chunk partition; changing batch partition is
incompatible, not asserted numerically equivalent. The local file lock is not
multi-host fencing. Controller publication/fencing remains the RUNTIME interface.

## Final-workspace parser and cache verification

Workspace: `outputs/workspaces/autonomy-candidate`, base
`e0eca9f9910244ecc20eb480d363f1852417b599`. These are freshly executed receipts.

The frozen fifth case now runs through the actual `_prompt_contract` scorer in
a bounded subprocess. Before the source fix it failed with `MemoryError` at
`data.quality._prompt_component_requirements` tuple expansion (128 MiB of
additional address-space allowance protects the reproducer). The original
identifier `generated_945137945819` was split into words, then its numeric suffix
was borrowed as a component multiplicity. The fix preserves identifier and
clause boundaries and never slices through a token. Explicit authored counts
remain exact: the regression retains all 32,769 requested Buttons. This does
not claim that arbitrarily large authored counts have bounded allocation cost.

The cache predicate now requires positive decoded document count, a complete
document/fragment partition, exact nonempty unique selected IDs, zero pending,
incomplete and timeout counts, and explicit measurement completion. Legacy
payloads missing selected-unit evidence cannot become successful cache entries.
No promotion or scientific threshold changed.

Current receipts under `outputs/runs/autonomy-mea-data/`:

| Receipt | Actual result | Evidence class |
| --- | --- | --- |
| `parser-before.xml` | 1 failed; reproducer API typo, subsequently corrected | Test authoring error, not product diagnosis |
| `parser-reproduced.xml` | 1 failed; actual frozen fifth-case `MemoryError` | Original-owner bounded reproduction |
| `parser-readiness-after.xml` | 80 passed, 4.31 s | Actual parser/scorer and trainer-readiness tests |
| `measurement-after.xml` | 110 passed, 1 stale positive fixture failed | New cache contract exposed missing fixture evidence |
| `measurement-complete.xml` | 111 passed, 8.62 s | Actual-owner contract/resume/loss/AgentV tests |

Commands use `rtk proxy timeout -s INT -k 10 170`, Python 3.12 from the existing
repository `.venv`, `PYTHONPATH=src`, `PYTHONDONTWRITEBYTECODE=1`, one Torch/BLAS
thread and `pytest -o addopts= -q --junitxml=<receipt>` (no marker exclusions).
Parser shard: `tests/test_data/test_semantic_plan_extraction/test_requirements_extract.py`,
`tests/test_evals/test_meaningful_program.py`,
`tests/test_harnesses/train_data/test_readiness.py`. Measurement shard:
`test_measurement_contract.py`, `test_eval_runner_resume.py`, `test_eval_gates.py`
under `tests/test_harnesses/model_build/`, plus `tests/test_evals/test_denoising_nll.py`.
Bridge and AgentV use the existing original checkout's `OPENUI_BRIDGE_CLI` and
`AGENTV_RUNNER` paths, not a copied cache or substitute SDK.

Integrator owns component bumps: `data.prompt_requirements`, `model.twotower`
(existing owner of `data/quality.py`), `evals.meaningful_program` for changed
prompt-scoring semantics, and `harness.model_build.eval` for cache/completeness.
The evaluator content identity already binds `data/quality.py`; pre-fix rows
must remain historical and post-fix measurements use a successor identity.
The current real six-case two-arm demonstration is pending, not replaced by
these tests. No service was installed, started, stopped, or restarted.

The explicit loss successor is `resources/evals/loss_suite_v2.json` (under
`src/slm_training/`). `LOSS_SUITE_VERSION` and the default loader now select v2.
Every v1 objective value is identical in v2; only version, evidence-contract
metadata and explanatory notes differ. The frozen v1 file is untouched and
`load_suite_spec("v1")` remains supported. Regression `loss-v2.xml`: 21 passed
in 1.83 s. Do not relabel old results v2 or pool across incompatible scorer IDs.

DATA's narrow controller integration is now executable:

```python
from slm_training.harnesses.train_data.readiness import lock_readiness_request

# request is DataReadinessRequest resolved from this activity's exact original
# snapshot, scored suites, checkpoint ancestry, resource minima and trainer cfg.
blocker.update(lock_readiness_request(store, request, experiment_id=experiment_id))
```

This creates one idempotent campaign event and content-addressed request. The
existing `data_rebuild.load_readiness_request` consumes the returned inline
payload directly; no manually authored mutable campaign sidecar is necessary.
Changing the same action's request is refused. The producer does not assert
readiness or close a blocker. Actual producer→consumer regression and readiness
shard: `data-producer.xml`, 20 passed in 1.99 s. Parent owns driver attachment.

The fresh preflight found additional data defects before training. The packaged
`e938_role_safe_all_targets_smoke96_v2` manifest declares empty SHA-256 values
for its nonempty adversarial and OOD record files. `readiness-report.json` records
the identity rejection; `leakage-diagnosis.json` independently records 52 of 893
train rows flagged against the actual smoke suite under existing leakage rules.
Neither result is training/model harm. The original data and manifest remain
unchanged. A local public-regression successor
`outputs/data/eval/autonomy-mea-smoke-successor-v1` corrects manifest integrity:
all non-manifest files are byte-identical, verified before and after publication
through the canonical data publisher. `eval-successor-action.json` binds the
original bytes, correction authority and non-modification requirement. This is
an explicitly authored diagnostic successor, not a deployed worker changing a
sealed confirmation suite. The first locked fixture campaign remains preflight-
incomplete; no experiment outcome was measured under that failed identity.

## Real data repair and six-case execution (final workspace)

The actual data-readiness demonstration is in
`outputs/runs/autonomy-mea-data/data-repair-demonstration.json`. The original
893-record request failed with 52 overlap findings. A sampling plan selected
the first original-order nonleaking record from eight actual source families,
before observing training outcomes. Strict canonical data construction admitted
all eight and rejected none; quality and synthesis-feedback reports were read.
Automatic language-contract/frontier additions were disabled at the source,
not by weakening admission gates. The immutable repaired training snapshot is
`outputs/data/train/autonomy-mea-train-admitted-v1`. Its independently rerun
readiness predicate passed: eight usable cases, eight declared families, actual
`TwoTower.from_records` preparation, and no neural forwards. This demonstrates
author-run canonical repair, not an unattended source-agent repair claim.

The initial full 893-record curation attempt exceeded its bounded invocation
(160.20 s, interrupted, no completed quality result). A preceding eight-seed
build with unwanted default producers admitted 113 of 118 candidates and is
retained but unused. The final source-restricted build admitted eight of eight
in 3.18 s. All attempts, rejection reports and source selections remain under
`outputs/runs/autonomy-mea-data/`; storage growth is not the readiness predicate.

Campaign `autonomy-mea-data-final-20260907` locked manifest
`dc723146a7b6c9a34304174207d3dbc1af3c2e6507ec3a0de6708d6bf7b30e8b`
before training. Two fresh CPU scratch TwoTower arms share seed 7301, width 32,
four heads, one context/denoiser layer, lexer/tree constrained decoding, three
updates and batch size two. Only learning rate differs (0.0003 vs 0.0006).
Both completed, in 7.30/7.38 seconds. Each consumed six examples, 1,536 prompt
tokens and 255 target tokens; exposure is 0.75 example epochs over eight rows.
Both saved immutable checkpoint bundles including full trial continuation
state. Initial argparse failures used a nonexistent positive full-state flag;
those receipts are retained and charged, not counted as training. The default
full-state behavior was then used without changing the declared recipe.

Evaluator v107 produced six decoded document rows per arm, no pending rows,
real AgentV result bundles, and exit 8 (measured ship-gate rejection, not a
missing scoreboard). The first control invocation was actually interrupted;
later invocations reused its committed row. Loss v2 completed all five
categories and actual AgentV publication for both arms: weighted conditional
masked-token CE 25.2254573574/24.6876548935, descriptive only. Six broad-loss
cases have explicit metadata roots; the four OOD cases lack declared root
identities and do not support an independence claim. These are public fixtures,
one shared initialization seed, not confirmation or production capability.

Independent direct-versus-resumed verification exposed a new failure despite
complete v107 scoreboards: the control's third-case prediction differed.
`batch-before.xml` reproduces the actual owner defect: a batch decoded two
records, but scoring the first could fail before the second prediction was
persisted. The fix commits all raw rows from a completed decode batch before
any fallible scorer call. `batch-after.xml` passed 24 tests in 2.19 seconds,
including recovery with zero new decoder calls. Old v107 outputs remain intact;
the corrected evaluator requires a successor identity and new decode evidence.
Final chunk/direct comparisons explicitly lock the same batch size and
partition, since different batch partitions are not a numerical-equivalence
claim. No retraining is needed for this measurement repair.

No service was installed or started, no remote writes occurred, and neither
checkpoint was promoted. The parent owns model-card/README composition and
the completed-original-campaign consumer integration.

The actual new NLL reports were remeasured after source-comment cleanup into
the reserved successor namespaces `runs/control-v108/loss_suites.json` and
`runs/candidate-v108/loss_suites.json`. The directory name is a planned decode
namespace, not a claim that decode v108 already ran: loss has its own v2
identity. Both completed with real AgentV output in 7.45/7.58 s, reproducing the
same weighted values. The canonical writer now adds an explicit loss/model
version stamp; `loss-report-stamp.xml` passed 22 tests. The writer's SDK mock
test proves only metadata plumbing; the two CLI results are the actual SDK
evidence.

`nll-only-attachment-proof.json` uses these measured rows through
`scripts.autotrain_nll.run_arm_eval_nll`, the parent's strict selection reader,
and canonical paired statistics. Both six-record diagnostic attachments remain
`measurement_complete=false`, `decoded_probe_complete=false`, and refused by
the decoded cache predicate. Six actual metadata-root pairs yield broad masked
CE 25.5114916348 versus 24.7525203287, mean paired improvement 0.7785168020,
median 0.7739877145, paired SD 0.0496898233, and exact two-sided sign-test
p=0.03125. This is a diagnostic fixture-screening result conditional on one
shared training seed and public roots, not independent training confirmation
or a semantic capability win. The first attachment inspection mistakenly
passed the selection reader's dict directly to `PairedSelection`'s consumer;
it failed before producing the proof report, then was corrected without
remeasuring or changing any row.

## Screening-generation lineage refusal

The current driver calls `extra_smoke_fixtures_for_deficit` from
`_append_missing_smoke_seeds`; previously policy resolution caught every
exception and omitted that training corpus. The caller also omitted actual
checkpoint ancestors. `screening-lineage-before.xml` reproduced four missing
refusals against the actual helper. `_policy_train_manifest` now uses
`DataStore.verify` and raises `ScreeningLineageUnavailable` on unavailable
policy/default data. Generation requires explicit `extra_train_manifests`;
`None` means undeclared lineage and is refused, whereas `()` explicitly
declares policy-only training with no additional ancestors. Missing files and
empty ID/prompt/program fingerprint evidence are refused before sampling or
mutating the caller's ID set. This does not authenticate worker-provided
manifests: the controller must supply content-bound paths from its locked
readiness request, and canonical post-publication readiness remains required.

Parent integration must pass the locked actual-training and all ancestor
manifest paths at `_append_missing_smoke_seeds`, catch the typed lineage error
as a scoped data prerequisite, and never insert an empty list merely to avoid
the refusal. The unchanged policy-only sampling regressions explicitly declare
their empty additional-ancestor contract. A redundant frozen-snapshot check
was removed (its preceding identical membership guard already raises), keeping
`screening_sample_size.py` at 800 lines without raising the 802-line ceiling.

Final combined owner verification after these changes:
`outputs/runs/autonomy-mea-data/final-domain-lineage.xml`, **228 passed in
31.23 seconds**, `pytest -o addopts= -q` with no marker exclusions. It includes
the seven previously listed domain files plus
`tests/test_autoresearch/test_screening_lineage.py` and
`tests/test_autoresearch/test_screening_sample_size.py`. Focused Ruff checks
passed. These are local domain receipts, not a repository-wide release receipt.
