# E52 semantic arity guard — 2026-07-15

The constrained picker now performs a semantic validation check only when a
candidate closes a component call. It rejects the specific `excess-args`
failure while leaving incomplete-prefix and unresolved-binding errors alone.

Focused grammar tests passed: 24. Strict E52 evaluation improved structural
similarity from 0.3569 to 0.4347, with parse still 0/3, raw syntax validity 0,
fidelity/reward 0, and p50 latency 25.5s. Predictions still contain malformed
symbol/literal adjacency, so the guard is helpful but not sufficient.

Decision: retain the arity guard and continue targeting native symbol/literal
closure.

This is strict scratch smoke evidence, not a ship claim.
