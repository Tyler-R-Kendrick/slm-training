# SLM-276 DCA2-03 — CausalDispositionBundleV1 (constrained-diffusion-causal-disposition-v1)

Outcome: **no_model_ship_ready_measurement_or_data_blocker**
Promotion action: `none_incumbent_retained`

## Prerequisite dispositions

- `SLM-268`: `blocked` (evidence: fixture_or_wiring)
- `SLM-269`: `blocked` (evidence: fixture_or_wiring)
- `SLM-270`: `blocked` (evidence: fixture_or_wiring)
- `SLM-271`: `blocked` (evidence: fixture_or_wiring)
- `SLM-272`: `blocked` (evidence: fixture_or_wiring)
- `SLM-273`: `blocked` (evidence: fixture_or_wiring)
- `SLM-274`: `blocked` (evidence: fixture_or_wiring)
- `SLM-275`: `blocked` (evidence: fixture_or_wiring)

## Hypothesis ledger

- **DCA-H1** (SLM-269): blocked — untested: SLM-269 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made
- **DCA-H2** (SLM-270): blocked — untested: SLM-270 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made
- **DCA-H3** (SLM-271): blocked — untested: SLM-271 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made
- **DCA-H4** (SLM-272): blocked — untested: SLM-272 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made
- **DCA-H5** (SLM-273): blocked — untested: SLM-273 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made
- **DCA-H6** (SLM-274): blocked — untested: SLM-274 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made
- **DCA-H7** (SLM-275): blocked — untested: SLM-275 prerequisite evidence is 'blocked'; the campaign never launched, so no causal claim of either sign is made

## Finalist eligibility

- `current_twotower_incumbent`: ineligible_no_vsd0_clean_scorer_or_qualified_baseline
- `autoregressive_legal_action`: ineligible_no_qualified_baseline_campaign
- `x22_valid_state_search`: ineligible_no_qualified_baseline_campaign
- `faithful_block_diffusion`: ineligible_not_activated_SLM-274
- `program_dpo_ipo`: ineligible_inconclusive_SLM-272
- `diffusion_rl`: ineligible_inconclusive_SLM-275

## Non-goals honored

No tournament spend (no eligible finalists), no promotion, no new architecture/objective/RL mechanism, no gate loosening, no fixture/local substitution. Blocked and not-activated results are recorded as valid completed dispositions.
