# E91 longer LTR-primary training — 2026-07-15

E91 extended E84's visible-contract, grammar-LTR-primary recipe from 128 to
512 training steps, then evaluated the resulting checkpoint in strict
no-fallback mode.

Strict smoke (n=1) remained syntactically invalid: parse/raw syntax 0.0,
structural similarity 0.5333, reward 0.0. Placeholder fidelity reached 1.0
with contract precision/recall 0.8/1.0. Latency was 6,344.46 ms with zero
timeouts, and both template fast-path/fallback counts were zero.

Decision: reject E91 for shipping, but retain it as evidence that longer
training improves contract fidelity and structure without yet producing a
parseable program. The next iteration should target the remaining native
symbol/binder sequencing failure.

This is strict scratch smoke evidence, not a ship claim.
