# E113 E91 recipe batch-one retry (2026-07-15)

E113 retried the E91 recipe with batch size `1` to avoid E112's resource
termination. The process again terminated before writing a checkpoint or
`train_summary.json`, this time at step `17`. Durable `metrics.jsonl` contains
the partial run telemetry through that step (3,381 prompt tokens and 1,257
target tokens).

No evaluation or quality claim is made. The repeated early termination across
batch sizes indicates an environment/resource failure in this larger recipe,
not a model result. Further retries should first add explicit launcher signal
capture/resource monitoring.
