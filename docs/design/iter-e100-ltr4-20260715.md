# E100 stronger LTR supervision (2026-07-15)

E100 retrained the visible-contract corpus with `ltr_loss_weight=4`, lexer
output, grammar LTR primary/repair, and 128 CPU steps. The corpus was held
constant at 1,417 records (manifest SHA
`c3ae5b41bb41010ee2d78b5b0c9bd257295234853ce6f9645c127fdca29e8dd3`).

Training completed with loss `20.2283`, 43,146 target tokens seen, and
17,821 ms total telemetry. The strict one-case smoke evaluation produced
parse `0.0`, raw syntax `0.0`, structural similarity `0.3`, contract
precision/recall `1.0/1.0`, placeholder validity `1.0`, and latency
`9178.83 ms`. AgentV emitted 5 failed checks with no execution errors.

Decision: reject E100 as a quality candidate. More LTR weight improved slot
contract adherence but worsened structure, so the next change should target
repair-state examples or loss composition rather than increasing LTR weight.
