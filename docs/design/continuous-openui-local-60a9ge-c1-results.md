# Autotrain c1 (continuous-openui-local, session 60a9ge): venv bootstrap gap, not a model result

**Verdict:** infrastructure failure, not scoreable. This session's ephemeral
container had a fresh Python 3.12 `.venv` that had not yet run
`scripts/setup_dev_env.sh`, so both the `control` and `-bounds` `wf_smoke_v2`
arms crashed inside `scripts/train_model.py` at `detect_device()` before any
training step ran. Neither arm produced a checkpoint, a scoreboard, or smoke
metrics; this is not evidence about the model or any lever.

> Note on campaign-id collisions: `outputs/autoresearch/` is gitignored and
> local to each ephemeral session container, so the driver's deterministic
> `continuous-loop-20260803-continuous-openui-local-8c0b60dd-c1` id has
> already been generated by other same-day sessions
> ([`autotrain-cycle-c1-torch-missing-infra-failure.md`](autotrain-cycle-c1-torch-missing-infra-failure.md),
> [`continuous-openui-20260803-c1-results.md`](continuous-openui-20260803-c1-results.md),
> [`continuous-openui-local-j48f8u-c1-results.md`](continuous-openui-local-j48f8u-c1-results.md)).
> This doc is this session's own, independent cycle 1 measurement under that
> same nominal id; the `60a9ge` branch suffix disambiguates the filename.

Unlike the earlier `autotrain-cycle-c1-torch-missing-infra-failure.md` session
(which hit a *bare* `ModuleNotFoundError` and had to fix `detect_device()`),
this session's `detect_device()` already fails closed with the actionable
message added in commit `72fdffa` (PR #1360), already merged to `origin/main`
at this cycle's `upstream_commit` (`2c4debe`):

```
RuntimeError: torch is not installed in this environment. Run
scripts/setup_dev_env.sh to install the pinned CPU wheel, or
`pip install --index-url https://download.pytorch.org/whl/cpu torch==2.5.1+cpu`
directly.
```

No new code change was required. The repair for this cycle was purely
operational: ran `scripts/setup_dev_env.sh`, which created `.venv` and
installed the pinned `torch==2.5.1+cpu` wheel plus the rest of the pinned dev
dependency set, matching CI.

Ship gates never ran (both arms failed before decode). No checkpoint was
created; nothing here is reusable, promotable, or ship evidence. Lean is
`not_applicable:screening`.

## SDLC Phase A

**Non-positive** (`harness_failure` / `infrastructure_failure_not_a_model_result`
on both arms; `primary_metric_unavailable`). Per `sdlc`
autotrain-iteration-delivery, no stacked PR layer is opened for this cycle —
local commit and docs only. The harness repair itself was already delivered
and merged in a prior, unrelated session (commit `72fdffa`), so this cycle's
`repair_harness` handoff action is acknowledged against that existing commit
rather than a new one.

Next: replay the identical frozen `-bounds` arm (`retry_measurement`) now that
`.venv` is bootstrapped with torch in this container.

Machine evidence:
[`continuous-openui-local-60a9ge-c1-results.json`](continuous-openui-local-60a9ge-c1-results.json).
