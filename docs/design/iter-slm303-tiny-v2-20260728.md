# SLM-303 tiny v2 fresh baseline — 600 steps on rebuilt fixture corpus

Run id: `slm303_tiny_v2_fresh` · Matrix set: none (ad-hoc autotrain loop) · Date: 2026-07-28 · Claim class: **fixture_demo / wiring only**

## What ran

- **Corpus rebuild (data-quality gate)**: the legacy `slm230_symbol_only_v1`
  fixture corpus no longer loads — current contracts reject named template
  markers (`:cta_aug.label`, …) and open strings in role-bound properties
  (`email`/`password` in `Input.name`). Rebuilt with the identical recipe
  (`build_train_data --source fixture --profile strict`) as
  **`slm230_symbol_only_v2`** (101 records, strict gates passed, lineage
  snapshot `2fe9535f…d1a478`, content fingerprint `fb320850…f21d10`).
- **Training**: fresh CPU scratch TwoTower (the `slm294_tiny_baseline`
  checkpoint is unloadable — vocab 505 vs frozen tokenizer layout 569 — so no
  resume/initialize-from was possible). 600 steps reached via 8 chained
  `--resume-from …/last_full_state.pt` segments under the immutable 170s
  command cap; every segment wrote a full-train-state checkpoint before the
  interrupt. Final `stopped_on: steps` (clean finish), final loss **2.4897**
  (6.96 → 2.49 over the chain), 101-record corpus, 1,608,962 trainable
  parameters, output contract v2, scratch context backend.
- **Eval**: `evaluate_model --test-dir outputs/data/eval/v1 --suite smoke
  --run-class fixture_demo`. The smoke suite is empty by construction
  (`records.jsonl` = 0 lines, same as the SLM-294 baseline eval), so
  `n=0` and no parse/fidelity metrics exist. This matches prior fixture
  evals exactly and is wiring evidence only.

## Honest disposition

- **Not promoted, not synced, not ship.** Fixture-corpus scratch checkpoint,
  no non-empty suite scored, no `--ship-gates` run (there is no populated
  suite to gate on locally).
- Checkpoint: `outputs/runs/slm303_tiny_v2_fresh/checkpoints/last.pt`
  (local, explicit `--no-sync-checkpoints`), sha256 `cae21423…de71b66d`.
- Resolving evidence for anything beyond wiring: a populated held-out suite
  compatible with output contract v2 + full scoreboard eval.

## Cycle 2 (same run id, continued): 1200 steps

- Training resumed through 11 more capped segments to **1200/1200 steps**
  (`stopped_on: steps`), final loss **0.585** (6.96 → 2.49 at 600 → 0.585 at
  1200). Same corpus, config, and 1,608,962-parameter model; checkpoint
  sha256 `336f9fc0…f08d1566`.
- Eval re-run on the final checkpoint (`fixture_demo`, smoke suite): still
  `n=0` — the suite is empty by construction. Disposition unchanged:
  **fixture wiring only, not promoted, not synced, not ship.**

## Cycle 3 (same run id, continued): 2400 steps

- Training resumed through 19 more capped segments to **2400/2400 steps**
  (`stopped_on: steps`), final loss **0.048** (6.96 → 2.49 at 600 → 0.585 at
  1200 → 0.048 at 2400). Same corpus, config, and 1,608,962-parameter model;
  checkpoint sha256 `25da750b…45bd1882`. The tiny fixture corpus is now
  essentially fit (as expected at 101 records — wiring evidence only).
- Eval re-run on the final checkpoint (`fixture_demo`, smoke suite): still
  `n=0` — the suite is empty by construction. Disposition unchanged:
  **fixture wiring only, not promoted, not synced, not ship.**

## Cycle 4 (same run id, continued): 4800 steps

- Training resumed through 51 more capped segments to **4800/4800 steps**
  (`stopped_on: steps`), final loss **0.014** (6.96 → 2.49 at 600 → 0.585 at
  1200 → 0.048 at 2400 → 0.014 at 4800). Same corpus, config, and
  1,608,962-parameter model; checkpoint sha256 `d697d564…`. Loss fluctuates
  in the 0.0–0.1 band — the tiny fixture corpus is fully fit (expected at
  101 records — wiring evidence only).
- Eval re-run on the final checkpoint (`fixture_demo`, smoke suite): still
  `n=0` — the suite is empty by construction. Disposition unchanged:
  **fixture wiring only, not promoted, not synced, not ship.**

## Artifacts

- `outputs/runs/slm303_tiny_v2_fresh/{train_summary,eval}.json`,
  `metrics.jsonl`, `checkpoints/last{,_full_state}.pt`
- `outputs/data/train/slm230_symbol_only_v2/` + lineage
  `outputs/lineage/data_snapshots/train-slm230_symbol_only_v2-*.json`
- Training log: `outputs/runs/slm303_v2_fresh.log`
