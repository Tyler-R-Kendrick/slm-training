# E700 — namespace role capacity

Date: 2026-07-21  
Status: completed negative; reverted; not ship

E700 isolates public string-property capacity after E698 likeness abstention,
without E699's confounding form aliases. The authoritative R3 full Held-out
`n=5` replay exited 0 within the three-minute cap, with constrained-only decode,
no timeout or fallback, and both AgentEvals JSONL and an AgentV SDK bundle after
all 147 compiler tests passed. R1 is excluded because fallback remained enabled
despite zero observed fallbacks; R2 is a constrained v167 rollback control, not
the v166 treatment.

| Held-out `n=5` | E699 r3 v163 | E700 v166 |
| --- | ---: | ---: |
| syntax / meaningful v1 | 1.0000 / 1.0000 | 1.0000 / 1.0000 |
| strict v2 / coverage | 0.8000 / 1.0000 | 0.8000 / 1.0000 |
| fidelity / validity | 0.9600 / 0.9760 | 0.9600 / 0.9760 |
| structure / component recall | 0.7724 / 0.8433 | 0.7724 / 0.8433 |
| reward | 0.9514 | 0.9514 |
| AST node / edge F1 | 0.8609 / 0.6888 | 0.8609 / 0.6888 |
| latency p50 / p95 | 3539.05 / 4741.01 ms | 3847.81 / 4884.53 ms |
| timeout / fallback | 0 / 0 | 0 / 0 |
| AgentV | 0/1 | 0/1 |

The focused planner test assigns a separate TextContent carrier after the
Callout exhausts its compatible string properties. That carrier is not emitted,
however: all five R3 predictions, including `held_out_form_01`, are byte-identical
to E699 r3 and `hint.title` remains missing. All aggregate quality metrics are
therefore identical; latency movement is not a performance claim.

Reject v166 and restore v165 behavior as v167. The next lever must ensure that
every planned role carrier becomes reachable from the final root aggregation,
not merely assign another family in the plan. No checkpoint was created,
synced, or promoted.

Evidence: [JSON](iter-e700-namespace-role-capacity-20260721.json).
