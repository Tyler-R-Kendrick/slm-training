# E108 seed variance control (2026-07-15)

E108 repeated the 128-step base visible-contract recipe with seed `1` instead
of the default seed `0`. The corpus and configuration were otherwise held
constant. Training completed at loss `5.91871`, with 42,574 target tokens and
full telemetry persisted.

Strict smoke evaluation remained invalid: parse/raw syntax `0.0/0.0`,
structural similarity `0.0`, contract precision/recall `1.0/1.0`, placeholder
fidelity `1.0`, component recall `0.0`, and latency `10166.99 ms`. AgentV
failed all five checks.

Decision: reject and conclude that seed variance does not explain the learned
Stack-list failure; the issue is systematic in this recipe.
