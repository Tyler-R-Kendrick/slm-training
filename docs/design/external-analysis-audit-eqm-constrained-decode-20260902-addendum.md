# Addendum S11: primary-source closure of the unverified EqM external quotes (2026-09-02)

Companion to
[`external-analysis-audit-eqm-constrained-decode-20260902.md`](external-analysis-audit-eqm-constrained-decode-20260902.md)
(Part B.1 rows 4, 9, 10, 13, 14, 15, 17; card S11). Research/docs only; no code.

Every row below was retrieved on **2026-09-02** from the URL named in the row.
Verdicts: **VERIFIED** (verbatim quote from the primary source),
**CONTRADICTED** (primary source says otherwise; the contradicting quote is
given), **UNRETRIEVABLE** (exact blocker given). Quotes are copied from the
retrieved text; where a fetch tool rendered a table row, the row is reproduced
as rendered and flagged as such. Nothing below is from memory.

Retrieval notes that apply to several rows:

- `arxiv.org/pdf/2602.02928v1` is 48.5 MB (29 pages) and exceeds the fetch
  tool's 10 MB cap; it was downloaded with `curl` into the scratchpad and the
  text extracted with `pypdf` (installed to a scratchpad-local target, not
  the repo venv). Page numbers below are PDF page indices from that extraction.
- `api.github.com` returned HTTP 429 to the fetch tool on every attempt and
  HTTP 403 through the session proxy (`"GitHub access to this repository is
  not enabled for this session"`); the GitHub MCP tool refused the
  unattached repository; attaching it with credentials was denied by the
  permission classifier. The issue threads were therefore read through the
  `r.jina.ai` reader rendering of the public `github.com` issue pages, which
  shows author usernames and dates but not the API's `author_association`
  field. The username `raywang4` is the repository owner (the repo lives at
  `github.com/raywang4/EqM`), so "author reply" below means "reply by the
  repo-owner account".

## Verdict table

| # | Claim under test | Verdict | Primary-source evidence (verbatim) | URL | Consequence for the audit |
|---|---|---|---|---|---|
| 1 | Distance Marching App. C.1 says EqM's objective "corresponds to a rescaled flow matching vector field", reports failed 2D reproductions with GD, GD+ULA, GD+HMC, and a landscape "inconsistent at two corner locations that are equally distant from the data manifold" | **VERIFIED** (all four sub-claims) | PDF p. 21, Appendix C.1 "2D Comparison with Energy-Based Models": "We next consider Equilibrium Matching (Wang & Du, 2025). Since it does not release a 2D model, we implement its objective by replacing our loss with theirs within the same 2D framework. In short, their learning objective corresponds to a rescaled flow matching vector field, and thus naturally inherits the issues discussed above. In 2D, this rescaling further amplifies the problem: the bottom of the inferred energy landscape becomes noisy, and the landscape remains inconsistent at two corner locations that are equally distant from the data manifold. We also try to reproduce their 2D generation using gradient-descent-based sampling, as they mentioned, but do not obtain a satisfactory result. In Fig. 20 and Tab. 8, we report three generation attempts: (i) Gradient descent only; (ii) Gradient descent + Unadjusted Langevin Algorithm (ULA); and (iii) Gradient descent + HMC." Fig. 20 caption: "We compare different generation methods on the Equilibrium Matching model, but none of them yields competitive results on this task." Tab. 8 (W2 / HD / CD): "Equilibrium Matching (Original GD) 1.433 2.034 0.260", "Equilibrium Matching (GD+ULA) 1.757 1.982 0.317", "Equilibrium Matching (GD+HMC) 2.073 1.680 0.037", "Distance Marching (ours) 1.435 0.605 0.005". Fig. 19 caption (p. 20): "Equilibrium Matching misses fine-grained energy structure near the data manifold and is inconsistent across two locations that are equally distant from the data manifold." Related work (Sec. 2): "Equilibrium Matching (Wang & Du, 2025) shows sensitivity to the choice of decaying coefficients with limited theoretical guidance, and its effectiveness drops when switching architectures or training setups." Only one arXiv version exists ("[v1] Tue, 3 Feb 2026 00:00:37 UTC (46,343 KB)"). | `https://arxiv.org/pdf/2602.02928v1` (text via pypdf); version list from `https://arxiv.org/abs/2602.02928`; HTML v1 `https://arxiv.org/html/2602.02928v1` does list heading "C.1 2D Comparison with Energy-Based Models" but the fetch renderer truncated before it | **Upgrades B.1 row 10 UNVERIFIED to VERIFIED.** Two caveats the report should carry: (i) this is a competitor's *re-implementation of the objective inside their own 2D framework*, not a run of released EqM code ("Since it does not release a 2D model, we implement its objective by replacing our loss with theirs"); (ii) the same table shows W2 is a tie (1.433 vs 1.435), so the "failed reproduction" is a HD/CD failure, not a uniform one. Evidence weight: independent but interested. |
| 2a | `raywang4/EqM` issue #5 has an author reply: "our derivations were based on the assumption that data is high dimensional, and it may not work as well on simple distributions" | **VERIFIED** | Issue #5 "Unable to Reproduce Results on Simple Distributions—Does EqM Require a Sparse (Dirac-Like) Target?" (opened by ischencheng, 2025-10-24, Closed). Reply by `raywang4`, 2025-10-24: "Indeed, our derivations were based on the assumption that data is high dimensional, and it may not work as well on simple distributions like this one." A later comment by `lukehouston85` (2025-11-04) adds: "the essential thing for training the EqM model is to make sure that margin gradient vanishes at real data points, so you need to manually add some points with t=1". | `https://github.com/raywang4/EqM/issues/5` (read via `https://r.jina.ai/https://github.com/raywang4/EqM/issues/5`); `https://api.github.com/repos/raywang4/EqM/issues/5/comments` gave HTTP 429 / 403 (see retrieval notes) | **Upgrades B.1 row 4 (part #5) UNVERIFIED to VERIFIED.** The quote is real and is the authors' own scope statement; it is consistent with, and independently corroborates, the Distance Marching 2D failure in row 1. |
| 2b | Issue #6 author reply says `forward_with_cfg` is "legacy code from the SiT paper" **and** that reported numbers used cfg 1.5 | **VERIFIED for the "legacy code" quote; CONTRADICTED as to where "cfg 1.5" comes from** | Issue #6 "Clarification regarding cfg" (Junoh-Kang, 2025-10-30, Closed). Opening post quotes `models.py:296-300`: "For exact reproducibility reasons, we apply classifier-free guidance on only three channels by default. The standard approach to cfg applies it to all channels." and asks "Are numbers in the paper are reproduced by cfg applied to only 3 channels?" The only reply, by `raywang4`, 2025-10-30: "Hi, this is legacy code from the SiT paper which we used as the baseline for Flow Matching. We used the exact same setting as the SiT baseline to ensure the comparisons are fair." The thread contains no mention of "1.5". The cfg-1.5 fact lives in the README results table instead: "\| EqM-B/2 \| 80 \| GD sampler, $\eta=0.003$ \| 1.0 \| 32.85 \|" and "\| EqM-XL/2 \| 1400 \| NAG-GD sampler, $\eta=0.0017$, $\mu=0.3$ \| 1.5 \| 1.90 \|". | `https://github.com/raywang4/EqM/issues/6` (via `r.jina.ai`, two fetches, identical thread); `https://raw.githubusercontent.com/raywang4/EqM/main/README.md` lines 83-86 | **Upgrades B.1 row 4 (part #6) to VERIFIED for the "legacy code" quote; corrects the attribution:** the audit and any citation must credit "cfg 1.5" to the README table (already B.1 row 1, VERIFIED), not to the issue #6 reply. The reply also does *not* answer the 3-channel question directly; it asserts parity with the SiT baseline. |
| 2c | Issue #13 author reply says dispersive loss (`--disp`) is "not required, nor is it used in the paper" | **VERIFIED** | Issue #13 "Is dispersive loss required?" (Junoh-Kang, 2025-11-25, Closed). Reply by `raywang4`, 2025-11-25: "No, it is not required, nor is it used in the paper. We used the DispLoss codebase to implement EqM, and the loss is just a legacy from the original code." (First reader fetch of the page rendered only the opening post; a cache-busting refetch and a second URL form both rendered the two-comment thread and the "Closed" state. README line 52 still shows `--disp` in the documented training command: `torchrun --nnodes=1 --nproc_per_node=N train.py --model EqM-XL/2 --data-path /path/to/imagenet/train --disp --wandb`.) | `https://github.com/raywang4/EqM/issues/13` (via `https://r.jina.ai/https://github.com/raywang4/EqM/issues/13?full=1` and `https://r.jina.ai/github.com/raywang4/EqM/issues/13`); README via `raw.githubusercontent.com` | **Upgrades B.1 row 4 (part #13) UNVERIFIED to VERIFIED.** Adds a reproducibility note: the documented training command enables a loss the authors say the paper did not use. |
| 3 | ICLR 2026 decision for OpenReview forum `xqlxtkOhDX`; reviewer ratings 4 and 8 with soundness 2 on one; reviews asked for Energy Matching / EBM comparisons and failure analysis | **UNRETRIEVABLE** | No review, rating, or decision text was retrievable from any source. Blockers, in order tried: `https://api2.openreview.net/notes?forum=xqlxtkOhDX` gave HTTP 302 to `https://openreview.net/challenge?redirect=...`; `https://api.openreview.net/notes?forum=xqlxtkOhDX` gave the same 302; `curl` with a browser UA on `api2...&details=replies` gave HTTP 403 with body `{"name":"ChallengeRequiredError","message":"Challenge verification required (2026-09-02-236521)","status":403,...}`; `https://openreview.net/forum?id=xqlxtkOhDX` gave a 4,787-byte "Verifying your browser" challenge page (fetch tool and curl); `https://openreview.net/pdf?id=xqlxtkOhDX` gave the same challenge; `https://r.jina.ai/https://openreview.net/forum?id=xqlxtkOhDX` gave "This page maybe requiring CAPTCHA"; `https://iclr.cc/virtual/2026/search?q=Equilibrium+Matching` returned a workshop listing with no paper hits; `https://iclr.cc/virtual/2026/papers.html?search=...` and `.../events/poster` are JS "Loading..." shells; `https://iclr.cc/virtual/2026/papers.json` is 404; `https://papercopilot.com/.../iclr-2026-paper-list/` rendered "Visible rows 0" and its `?s=` form is 404; Semantic Scholar `fields=venue,publicationVenue` gave HTTP 429 twice. Non-primary signals only (not quotable as verified): the arXiv abstract page, the authors' project page, alphaxiv, and the Hugging Face paper page all state **no venue**; a search-engine snippet for `openreview.net/pdf?id=xqlxtkOhDX` describes the header as under review at ICLR 2026, and search metadata gives the submission as 2025-09-18, modified 2026-02-11. The ICLR 2026 virtual site does list a *different* paper, "Generative Associative Memory via Equilibrium Matching" (Rodriguez, Hoover, Guo, Du) at `https://iclr.cc/virtual/2026/10022430`; it must not be conflated with `xqlxtkOhDX`. | see blockers | **B.1 row 14 stays UNVERIFIED.** The report's "reviews 4 and 8", "soundness 2", "absent from the program", and "reviewers requested Energy Matching / EBM comparisons" are all uncited and must not be repeated in repo docs until someone with a logged-in OpenReview session reads the forum. The only defensible statement is: "no venue is claimed on arXiv, the project page, or the HF paper page as of 2026-09-02". |
| 4 | Semantic Scholar citation count for arXiv:2510.02300 is about 21 | **VERIFIED** (exactly 21) | HTTP 200 response body: `{"paperId": "165d353ec77cf232562165d26f212ae18b8ce2cf", "title": "Equilibrium Matching: Generative Modeling with Implicit Energy-Based Models", "citationCount": 21, "influentialCitationCount": 4}`. Retrieved via `curl` with backoff; the first attempt of the day succeeded, later follow-up queries (venue fields) hit HTTP 429. | `https://api.semanticscholar.org/graph/v1/paper/arXiv:2510.02300?fields=citationCount,title,influentialCitationCount` | **Upgrades B.1 row 15 UNVERIFIED to VERIFIED** (21 on 2026-09-02; 4 "influential"). Citation counts drift; cite with the date. |
| 5 | LightningDiT / VA-VAE reports ImageNet-256 FID 1.35 **without** CFG (critique's claim) | **CONTRADICTED** (1.35 is *with* CFG; 2.17 is without) | Table 3 of arXiv:2501.01423, as rendered by the fetch tool (columns as rendered): generation w/o CFG row "LightningDiT \| VA-VAE \| 0.28 \| 800 \| 675M \| 2.17 \| 4.36 \| 205.6 \| 0.77 \| 0.65"; with-CFG row "LightningDiT \| VA-VAE \| 0.28 \| 800 \| 675M \| ... \| 1.35 \| 4.15 \| 295.3 \| 0.79 \| 0.65". The fetch tool's own reading: "FID 1.35 (with CFG)" and "FID 2.17 (without CFG) ... (middle section of the table labeled 'Generation w/o CFG')". Abstract: "achieves state-of-the-art (SOTA) performance on ImageNet 256×256 generation with an FID score of 1.35 while demonstrating remarkable training efficiency by reaching an FID score of 2.11 in just 64 epochs." The abstract page (`/abs/`) does not mention CFG at all. | `https://arxiv.org/html/2501.01423` (Table 3); `https://arxiv.org/abs/2501.01423` | **Upgrades B.1 row 13 LIKELY WRONG to CONTRADICTED.** The report's omitted-baseline point survives (LightningDiT 1.35 with CFG beats EqM-XL/2 1.90 with cfg 1.5; LightningDiT 2.17 without CFG is a number the EqM paper has no unguided counterpart for), but the label "without CFG" is wrong and must be corrected wherever the report is quoted. |
| 6 | arXiv:2510.02300 v2 introduced adaptive-NAG pseudocode (Algorithm 2) with an inverted loop condition `while norm(grad) < g`, and v3 changed it to `> g` | **VERIFIED** | Version list: "[v1] Thu, 2 Oct 2025 17:59:06 UTC", "[v2] Thu, 9 Oct 2025 15:25:23 UTC", "[v3] Mon, 13 Oct 2025 15:39:31 UTC". v1 Algorithm 2 "Equilibrium Matching Sampling" is a fixed-step loop: `def generate(f, st, eta, N): xn = st; for i in range(N): xn = xn - eta*f(xn); return xn` — no adaptive variant and no `while` on a gradient norm (v1 Sec. 5.2 prose already says sampling proceeds "until ‖∇E(x_k)‖₂ < g_min"). v2 Algorithm 2 "Adaptive EqM Sampling (NAG)", extracted from the HTML: `def generate ( f , st , eta , mu , g ): x = st , x_last = st , grad = f ( st ) while norm ( grad ) < g : x_last = x x = x - eta * grad grad = f ( x + mu *( x - x_last )) return x`. v3, same block: `while norm ( grad ) > g :` — the only difference. Both v2 and v3 prose say the stop is "when the gradient norm drops below a certain threshold g_min" / "until ‖∇E(x_k)‖₂ < g_min", so the v2 pseudocode contradicted its own prose (the loop would exit before the first step whenever the initial gradient norm is at or above the threshold). v2 and v3 HTML are byte-identical in size (306,108 B); v1 is 301,125 B. | `https://arxiv.org/html/2510.02300v1`, `https://arxiv.org/html/2510.02300v2`, `https://arxiv.org/html/2510.02300v3` (downloaded with curl; tag-stripped and grepped for `while norm`); version list from `https://arxiv.org/abs/2510.02300v2` | **Upgrades B.1 row 17 UNVERIFIED to VERIFIED.** It also strengthens B.1 row 2 (released `sample_gd.py` has no adaptive-stop argument): the adaptive sampler was added to the paper a week after v1, shipped with a sign error for four days, and is still absent from the released sampler. |
| 7 | SR-DiT App. B.4 exact sentence on Equilibrium Matching, and the architecture it was tried on | **VERIFIED** | arXiv:2512.12386v1, Appendix "B.4 Alternative Training Objectives", paragraph "Equilibrium Matching.": "We attempted to implement Equilibrium Matching [32], which combines energy-based modeling with flow matching. Despite following the methodology, we were unable to reproduce their reported results on our SiT-B/1 architecture with representation alignment. Performance was significantly worse than standard flow matching, suggesting potential incompatibilities between EqM and our architectural choices or training setup." The immediately preceding paragraph fixes the setting as latent-space with their own tokenizer: "However, in our latent space setting with INVAE, performance was significantly worse than standard flow matching." | `https://arxiv.org/html/2512.12386` (resolves to v1; tag-stripped and grepped) | **B.1 row 9 stays VERIFIED; the caveat is now sourced.** The failed attempt was on SiT-B/1 (patch size 1) with representation alignment and an INVAE latent space, and the authors themselves attribute it to "potential incompatibilities ... with our architectural choices or training setup". It is a negative signal about transfer, not a reproduction failure of the published configuration. |

## Net effect on the main audit (B.1)

- VERIFIED now: rows 4 (#5, #13, and the "legacy code" half of #6), 10, 15, 17.
- CONTRADICTED now: row 13 (critique's "1.35 without CFG"); the "#6 reply says cfg 1.5" attribution in row 4.
- Still UNVERIFIED: row 14 (ICLR 2026 decision and scores) — blocked by OpenReview's browser challenge on every API and page route; needs a logged-in human.
- The report's headline external claims that survive with primary sources: the authors' own high-dimensional scoping (#5), dispersive loss not used but enabled in the documented command (#13), the Distance Marching 2D failure (with the re-implementation caveat), the SR-DiT transfer failure (with the SiT-B/1 + REPA + INVAE caveat), the v2-to-v3 sign fix in Algorithm 2, and the cfg-1.5 disclosure gap (README, not the issue thread).

## Method record

- Fetch tool for arXiv abs/HTML, the `r.jina.ai` reader, Semantic Scholar, ICLR, papercopilot, alphaxiv, Hugging Face; web search for venue discovery only (search summaries were never used as evidence).
- `curl` from the scratchpad for: the 48.5 MB Distance Marching PDF (`pypdf` text extraction), the three EqM HTML versions (tag-strip + regex), the SR-DiT HTML, the EqM README, the Semantic Scholar JSON, and the OpenReview challenge probe.
- No repository code, tests, or resources were touched; no version-stamped component changed (doc-only addendum).
