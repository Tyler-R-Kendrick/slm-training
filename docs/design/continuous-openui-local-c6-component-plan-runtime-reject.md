# continuous-openui-local c6 — frozen replay runtime-rejects component-plan

Frozen retry of c5: control completes; candidate-only decode timeout **reproduces**.
Climb **rejected** via `candidate_runtime_rejected_after_frozen_replay`. Next thrash arm rotates away from component-plan.

Policy decode-budget raise (c5 repair) applies to *new* thrash recipes; frozen knobs kept 12s by design.
