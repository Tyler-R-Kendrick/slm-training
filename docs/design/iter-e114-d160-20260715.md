# E114 intermediate-size recipe retry (2026-07-15)

E114 kept the E91 high-quality recipe settings but reduced the model to
160 dimensions / 5 heads / 3 context layers / 5 denoiser layers and batch size
2. The process terminated at step `12` without a checkpoint or summary.

Partial durable telemetry remains in
`outputs/runs/iter-e114-d160-20260715/e114_d160/metrics.jsonl` (4,870 prompt
tokens and 1,836 target tokens). No evaluation or quality claim is made.

Decision: incomplete/resource-terminated. The environment cannot currently
support the higher-quality model envelope; pivot to the small model while
adding explicit resource/exit diagnostics before retrying larger recipes.
