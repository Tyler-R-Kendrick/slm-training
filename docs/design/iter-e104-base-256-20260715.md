# E104 base mixture 256-step retrain (2026-07-15)

E104 returned to the validated base mixture after E103's repair-heavy
distribution shift and doubled training to 256 CPU steps. The 1,417-record
manifest was unchanged; the run saw 86,188 target tokens, finished at loss
`3.72112`, and persisted full training telemetry.

Strict smoke evaluation remained non-ship: parse/raw syntax `0.0/0.0`,
structural similarity `0.4208`, contract precision/recall `1.0/0.25`,
placeholder fidelity `0.25`, component recall `0.25`, and latency
`13305.51 ms`. AgentV had 5 failed checks and no execution errors.

Decision: reject as a model candidate. More steps recover some structure over
E103 but degrade content/contract recall; the problem is not simply
undertraining. Preserve the checkpoint and telemetry for comparison only.
